# Kite Project Development Rules (React + Shadcn UI Edition)

你现在是 Kite 项目的首席架构师。Kite 是一个由 Go 驱动、极致扁平化（Flat Design）的 AI 原生博客引擎。在编写代码时，必须严格遵守以下规则：

## 1. 核心架构与目录规范
- 语言栈: 后端 Golang (Gin + GORM), 前台 SSR (Go Template), 后台管理 SPA (React + Vite + Tailwind CSS)。
- 物理布局:
  - 核心逻辑严禁离开 `internal/` 目录，确保包的私有性。
  - 静态资源统一通过根目录的 `embed.go` 利用 `go:embed` 模式打包。
- 双模逻辑: 逻辑分支必须判断 `config.RenderMode`。
  - `classic`: 走 `internal/render` 吐出 HTML 模板。
  - `headless`: 仅输出标准的 JSON 响应。

## 2. 数据库准则 (High Priority)
- 唯一标识: 所有模型（Article, Tag 等）主键必须使用 `uuid.UUID`。严禁使用 `uint` 或自增 ID。
- 跨库兼容: 字段定义必须同时适配 SQLite (local) 和 PostgreSQL (prod)。
- GORM 钩子: 必须在 `BaseModel` 中定义 `BeforeCreate`，确保在插入前执行 `b.ID = uuid.New()`。

## 3. 前端 UI 与视觉规范 (Shadcn UI 极简风约束)
后台 UI 必须复刻类似 Halo 2.x 的现代化、克制、扁平视觉风格。生成 React 组件时，必须遵循以下样式硬性规定：
- 颜色与层级逻辑:
  - 整体应用底色使用极浅灰（如 `bg-slate-50` 或 `bg-zinc-50`）。
  - 所有核心内容容器（侧边栏、顶部导航、主内容卡片、编辑器区域）必须使用纯白背景 `bg-background` (`#FFFFFF`)。
  - 层级划分严禁使用常规阴影，必须统一使用极细边框（如 `border-r`, `border-b`, `border border-border`）进行物理分割。
- 字体与排版:
  - 默认字号偏小，大量使用 `text-sm`，辅助信息使用 `text-xs text-muted-foreground`。
  - 增加组件间的呼吸感，大量使用 `gap-4`, `p-6` 等间距类。
- Shadcn 组件使用约束:
  - Button: 默认操作使用 `variant="default"`，次要操作必须使用 `variant="outline"` 或 `variant="ghost"`。严禁使用带厚重阴影的按钮。
  - Card: 移除 Shadcn 默认 Card 的阴影，重写为 `className="border border-border bg-background rounded-md shadow-none"`。
  - 悬浮层 (Dropdown/Popover/Command): 仅在这些脱离文档流的浮层组件（如 AI 写作助手菜单）上使用轻量阴影 `shadow-sm` 或 `shadow-md`。
- 交互与状态:
  - 侧边栏和列表项的悬停状态统一使用 `hover:bg-accent hover:text-accent-foreground`。
  - 保持圆角克制，全局统一使用小圆角 `rounded-md`，禁用 `rounded-full`（除非是头像 Avatar）。

## 4. 后端开发规范
- 防御性编程: 显式处理所有 `error`。API 统一响应结构：`{ "code": 200, "data": T, "msg": string }`。
- 解耦: 业务逻辑下沉到 `service` 层。预留 AI 接口抽象，以便接入 DeepSeek/OpenAI 处理文章摘要和 AI 写作辅助功能。
- 配置: 禁止硬编码。所有配置项（端口、DB 路径、LLM Key）必须通过 `internal/config` 读取。

## 5. 自动化与协作
- 构建同步: 修改 Go 模型后，提醒执行 `make build-server`；修改 React 源码后，提醒执行 `make build-ui`。
- 依赖维护: 保持 `go.mod` 处于 tidy 状态，保持 `package.json` 依赖整洁。